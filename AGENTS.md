# EasyPingMake 项目开发规范

## 1. 项目定位

EasyPingMake 是一个持续迭代的 AI 拼豆图纸生成产品。

项目目标：

帮助用户将真实图片转换为可制作的拼豆图纸。

核心流程：

图片输入 → AI 图像生成/处理 → 像素化转换 → 拼豆色板映射 → 图纸输出

当前阶段：

优先完成 MVP 版本，用于验证核心产品流程。

长期目标：

在保持代码可维护性的基础上，根据产品需求逐步扩展功能。

------------------------------------------------------------------------

## 2. 文档规范

项目文档用于保持产品、设计和开发之间的信息同步。

主要文档：

### 产品需求文档

路径：

`docs/PRD.md`

作用：

-   定义产品目标
-   定义用户需求
-   定义当前阶段功能范围
-   功能列表

### 设计规范

路径：

`docs/design.md`

作用：

-   定义 UI 风格
-   定义视觉规范
-   定义交互原则

### 原型图

路径：

`docs/prototype/`

作用：

作为页面结构和交互参考。

原型图不是不可修改的限制。如果开发过程中发现技术限制、用户流程问题或更优方案，应提出调整建议，并同步更新相关文档。

------------------------------------------------------------------------

## 3. 文档同步规则

项目文档代表当前阶段的产品共识。

开发过程中，如果发现：

-   原需求无法合理实现；
-   当前方案影响用户体验；
-   技术方案需要调整；

应遵循：

1.  说明问题。
2.  提出修改方案。
3.  确认调整方向。
4.  更新相关文档。
5.  修改代码实现。

不要：

-   只修改代码，不同步文档。
-   未沟通情况下改变产品行为。

PRD 会随着产品迭代更新，不要求永久保持不变。

------------------------------------------------------------------------

## 4. 开发原则

### MVP 优先

当前版本优先验证核心用户价值。

开发时：

-   优先完成核心流程闭环。
-   避免提前实现低优先级功能。
-   选择简单可靠的实现方式。

如果存在多个方案：

优先选择当前阶段最简单、可验证、方便迭代的方案。

### 保持可扩展性

代码设计需要：

-   模块职责清晰；
-   数据和逻辑分离；
-   方便未来扩展。

避免：

-   大量硬编码；
-   一次性脚本；
-   难以维护的临时代码。

### 避免过度工程化

未经产品需求确认，不主动引入：

-   复杂基础设施；
-   不必要依赖；
-   与当前目标无关的功能。

未来可以根据新的 PRD 逐步增加功能。

------------------------------------------------------------------------

## 5. 开发流程

实现新功能前：

### 需求理解

阅读相关资料：

-   PRD
-   design
-   prototype

确认：

-   功能目标；
-   输入输出；
-   验收标准。

### 方案分析

编码前说明：

-   实现思路；
-   涉及文件；
-   数据流变化；
-   潜在风险。

重大架构修改需要先讨论。

### 功能实现

要求：

-   完成最小可运行版本；
-   不修改无关代码；
-   保持代码结构清晰。

### 验证

完成后说明：

-   修改内容；
-   测试方式；
-   测试结果；
-   遗留问题。

------------------------------------------------------------------------

## 6. Git 规范

项目使用 Git 进行版本管理。

Commit 信息格式：

-   `feat:` 新增功能
-   `fix:` 修复问题
-   `refactor:` 代码重构
-   `docs:` 文档修改
-   `chore:` 配置修改

示例：

    feat: add image upload module
    fix: correct palette mapping
    docs: update PRD

要求：

-   保持提交粒度清晰。
-   大功能完成后及时提交。
-   不随意修改历史提交。
-   不执行破坏性 Git 操作。

------------------------------------------------------------------------

## 7. 代码规范

代码要求：

-   简洁；
-   易读；
-   易维护。

遵循：

-   使用明确命名；
-   保持模块单一职责；
-   避免重复代码。

### 数据与代码分离

业务数据应该独立管理。

例如：

    data/
    └── colorSystemMapping.json

不要：

-   将大量业务数据写入代码；
-   将配置参数硬编码。

------------------------------------------------------------------------

## 8. AI 模块规范

AI 能力应该保持模块化。

推荐：

    用户输入
        ↓
    AI Service
        ↓
    Image Processing
        ↓
    Pattern Generator
        ↓
    Output

要求：

-   AI 调用逻辑不要直接写在 UI 层。
-   Prompt 和模型参数应方便调整。
-   保留未来替换模型的能力。

------------------------------------------------------------------------

## 9. 测试规范

每个功能完成后至少验证：

-   正常流程；
-   异常情况；
-   输入限制。

图像处理模块：

-   输入输出需要明确；
-   尽量保证结果稳定。

自动化测试：

不要调用收费外部 AI 服务。

------------------------------------------------------------------------

## 10. 范围控制

当需求不明确时：

优先：

1.  查询已有文档。
2.  选择符合当前目标的最简单方案。
3.  说明假设。

不要：

-   自行增加产品功能；
-   改变核心流程；
-   扩大项目范围。

最终目标：

构建一个从产品设计、AI 能力验证到工程实现的完整、可持续迭代的 AI
产品项目。
